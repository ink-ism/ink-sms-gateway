# 开发环境配置
spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.ink.admin: DEBUG
    org.springframework.cloud: DEBUG
    org.mybatis: DEBUG
